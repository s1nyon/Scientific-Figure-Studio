# Scientific Figure Studio 工作规范

本文件适用于项目根目录及其子目录。它是四个项目 Skill 和后续绘图任务的公共约束。

## 数据真实性

- 不编造实验数据、模型结果、评价指标、置信区间或统计结论。
- 不为了视觉效果删除、覆盖、隐藏或改变原始数据和异常值。
- 任何平滑、过滤、聚合、归一化、插值、坐标变换和不确定性计算都必须在源码、manifest 或 README 中记录。
- 示例数据必须明确写成 `illustrative practice data`，不得冒充比赛结果。
- 图表中的优势、趋势或因果关系只能来自输入数据和已说明的数学处理。

## 个人图库保护

- 不删除、覆盖、移动、重命名或上传用户收藏的原始图片。
- 自动生成的索引、缩略图和设计笔记必须保存在与原图分开的生成目录。
- 不自动把图片放入 `04_my_favorites`，不编造用户评价和个人审美偏好。
- 无法从图片确定的字体、数据、结论和来源必须标记为未知或估计。
- 重复图片只做哈希标记，不删除任一原文件。

## 源码强制交付

- 每次正式绘图都必须同时交付完整、可运行、可手动修改的 Python 源码。
- 常用画布、颜色、线宽、字号、标记、图例、坐标范围、间距和输出格式应集中在 `config.py`。
- 图片、源码、配置和数据 manifest 必须由同一版本生成，修改源码后必须重新渲染。
- 不依赖 Notebook 隐藏状态、开发者本机绝对路径或对话中未保存的代码片段。

## 视觉规范

- 优先使用 `figure_studio` 的样式、配色、字体和布局接口。
- 同一算法或模型在相关图中尽量保持同色，并配合线型、标记或直接标签传递类别信息。
- 选择与数据语义匹配的分类、连续或发散色图，不默认使用 `jet` 或 `rainbow`。
- 图表应保留科学上必要的坐标轴、刻度、单位、图例和标注；极简不等于删除信息。
- 视觉层次应突出主要证据，不能通过颜色、坐标比例或装饰制造数据不支持的优势。
- 导出优先保留 PDF/SVG 矢量结构和可编辑文字，同时提供高分辨率 PNG 预览。

## 科学表达

- 先写 Figure Brief：数据来源、字段、变量含义、单位、目标、版面尺寸和不确定性来源。
- 图表类型必须符合数据结构和数学问题；例如帕累托前沿必须使用真实目标方向计算非支配关系。
- 预测图必须区分训练、验证和测试数据；误差指标只能从实际展示的数据计算。
- 没有真实依据时不绘制置信区间、波动带、趋势线或统计显著性。
- 对数坐标遇到零或负值时必须显式处理并记录，不能静默删除或改变数据。

## 工作流程

正式绘图按以下顺序进行：数据检查 → Figure Design → Python 实现 → 实际运行 → 打开实际图片 → 修复布局或表达问题 → 交付图片、源码、配置和说明。

视觉审查必须尽可能查看渲染后的图片，并检查最终插入尺寸下的可读性。不能只读代码就声称图片通过审查。

## 代码和依赖

- 使用 pathlib 处理路径，保持 Windows 和 WSL/Linux 兼容。
- 优先 Matplotlib 面向对象 API，模块只承担清晰职责。
- 使用项目 Conda 环境运行 Python、pytest 和 Ruff，不改变系统 Python。
- 不直接执行未经审查的第三方 Skill 脚本，不安装与当前绘图目标无关的大型依赖。
- 不使用破坏性 Git 命令覆盖用户工作。

## 测试和报告

- 测试必须实际运行；未执行的检查标记为 `未验证`，失败的检查如实记录。
- 自动测试覆盖导入、样式、数据错误、导出文件、配置生效、字体、图库和 Skill 结构。
- 六张示例图必须经过实际渲染和视觉检查。
- 测试报告必须区分：已完成代码、已实际运行、已通过自动测试、已实际检查图片和仍需用户确认。

## 完成定义

只有同时满足以下状态才可声称第一版完成：

1. Conda 环境可运行项目代码。
2. 六类模板实际生成 PDF、SVG、PNG，并交付源码、配置和 manifest。
3. 图库可以处理空目录、增量扫描、哈希去重和检索，并保护原图。
4. 四个 Skill 的元数据、引用路径和职责可检查。
5. 自动测试和 Ruff 的真实结果已经记录。
6. 六张图片和中文字体测试图已经实际查看。
7. 未验证项和需要用户人工确认的内容已经单独列出。
