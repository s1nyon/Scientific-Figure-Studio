# Scientific Figure Studio 工作规范

本文件适用于项目根目录及其子目录。它是 Agent 进入项目后必须遵守的公共约束；具体图形设计仍由当前科学任务、真实数据和适用的专业 Skill 决定。

## 当前项目定位

Scientific Figure Studio 当前处于 **Nature-first 科研绘图、个人案例积累、高质量 Figure 生成与验收** 阶段。项目模板和样式系统是可复用实现工具，不是统一绘图入口；六类模板的历史成功渲染也不是当前项目的整体完成标准。

## 科学准确性

- 不编造原始数据、模型结果、评价指标、置信区间、误差带或统计结论。
- 不为了视觉效果删除、覆盖、隐藏或改变原始数据和异常值。
- 不擅自改变目标函数方向，不把单次实验现象推广为算法普遍性能。
- 不把最终目标函数值与收敛速度、稳定性、鲁棒性或统计显著性混为一谈。
- 任何平滑、过滤、聚合、归一化、插值、坐标变换和不确定性计算都必须在源码、manifest 或 README 中记录。
- 必须核对图中使用的数据、计算方法、单位、目标方向和重要标注。真实比赛数据的科学含义不明确时先确认；不能自行补造。
- 练习数据和示例结构必须明确标记为 `illustrative practice data` 或 `illustrative example structure`，不得冒充正式结果。

## Skill 分工

- 固定版本的原版 `$nature-figure` 在适用范围内主导 Figure Contract、证据层级和专业设计指导；不得复制、修改或用本地文档替代其正文。
- `$modern-scientific-figure` 是本项目的唯一统一绘图入口，负责理解任务、建立 Figure Brief、分流专业能力、选择 Python 实现、执行和交付。
- `$algorithm-visualization` 负责收敛、预测误差、敏感度、鲁棒性、Pareto、空间路径和网络等数学语义检查。
- `$scientific-illustration` 负责流程图、模型架构、几何、三维、网络机制和图表/示意图组合。
- `$figure-reference-manager` 负责个人图库扫描、视觉分析、检索和作品关联。
- `$figure-design-review` 负责打开实际图片后的布局、可读性、导出和源码精修。
- 现有 Python 模板、`figure_studio` 样式和布局接口均为可选实现工具。不得为了使用模板或预设而改变数据、目标方向、模型结构或证据关系；不要要求每个任务调用全部 Skill。

## 图库和作品保护

- 不删除、覆盖、移动、重命名或上传用户收藏的原始图片、历史测试成果和已认可作品。
- 自动生成的索引、缩略图、视觉分析和设计笔记必须保存在与原图分开的生成目录。
- 不自动把图片放入 `04_my_favorites` 或 `05_my_work`，不编造用户评价和个人审美偏好。
- 文件扫描事实、Agent 实际打开图片后的视觉观察、用户明确评价、作品科学内容、代码实现和复现 provenance 必须分开记录。
- 无法从图片确定的字体、数据、结论、来源和授权状态必须写为未知或估计；本地存在第三方图片不代表拥有公开再分发权限。
- 重复图片只做哈希标记，不删除任一原文件。

## 源码和交付

每次正式绘图都必须提供同一版本生成的：

- 实际生成的 PNG 预览，以及可编辑文字/矢量结构优先的 SVG、PDF；
- 完整、可运行、可手动修改的 Python 源码和配置；简单图尽量提供易读的独立 `plot.py`，复杂 Figure 可模块化但必须能独立复现；
- 必要输入数据或结构素材、`data_manifest.json`、Figure Brief、Design Receipt、运行 README 和版本/哈希记录；
- 真实执行、图片查看、精修动作和仍未验证项目的说明。

配置应集中管理画布、颜色、线宽、字号、标记、图例、坐标范围和输出格式。不得依赖 Notebook 隐藏状态、开发者本机绝对路径或对话中未保存的代码片段。源码修改后必须重新渲染全部交付格式。

## 正式绘图流程

1. 检查数据、模型/结构定义、单位、目标方向、数据划分和不确定性依据，记录未知信息。
2. 建立 Figure Brief，先写清当前图要表达的可验证结论、主要证据、必要辅助证据、预期尺寸和审查风险。
3. 对适用的数值图或综合 Figure 显式加载固定版本 `$nature-figure`，再按需使用专业 Skill 和少量匹配的个人案例。
4. 根据科学任务选择图形类型、信息层级、面板数量和 Python renderer；不要仅凭字段数量或历史作品决定构图。
5. 实际运行源码，导出 PNG/SVG/PDF，并保留输入、配置和 provenance。
6. 打开实际图片，在原始尺寸和论文预期插入尺寸检查科学表达、阅读顺序、裁切、重叠、字体、单位、图例、标注、颜色和导出。
7. 发现整体结构问题时重新评估 Figure 方案；发现局部问题时优先修改 `plot.py`/`config.py`，然后重新渲染和查看。
8. 交付完整复现包。用户提出局部修改时先保存可恢复快照，并生成 Before/After；只有用户明确认可候选版本后，才可提升为 accepted work。

三种工作模式按任务选择：`ordinary` 自主完成一个方案，`core-practice` 才生成两种具有实质信息组织差异的草稿，`formal` 先完成完整初版和检查再交给用户验收。不要强制所有任务生成两种方案。

## 验收状态不可替代

报告时分别标记：

- **A 程序状态：** 源码运行、导出和自动测试结果；
- **B 科学状态：** 数据、计算、单位、目标方向和图形表达是否经过检查；
- **C 视觉状态：** 实际图片是否已打开，并完成最终尺寸基础审查；
- **D 用户验收：** 用户是否认为作品达到其个人审美要求；
- **E 个人收藏：** 用户是否明确认可并将其保存为个人案例。

pytest、Ruff、SVG 导出或 Design Receipt 通过，只能证明相应程序/记录状态，不能替代 B、C、D 或 E，也不能证明顶刊视觉品质。用户认可的 Design A 是有明确范围和限制的个人参考案例，不是全局模板、质量认证或普遍审美偏好。

## 安全执行与历史记录

- 使用 `pathlib` 保持 Windows 和 WSL/Linux 兼容；优先 Matplotlib 面向对象 API；不安装与当前任务无关的大型依赖。
- 使用项目 Conda 环境运行 Python、pytest 和 Ruff；实际未执行的检查标为“未验证”，失败结果如实记录。
- 不使用破坏性 Git 命令覆盖用户修改；开始任务前检查分支、HEAD、远端状态和工作区；优先在独立分支或 worktree 工作，不擅自合并主分支。
- `README.md`、`QUICK_START`、设计指南和当前 Skill 是新任务的工作规范。P0/P1/Phase 2/Phase 3 计划、审查报告和测试报告只记录当时状态与证据，不能直接当作当前事实或主要指令来源；不要求每次任务阅读全部历史文档。
