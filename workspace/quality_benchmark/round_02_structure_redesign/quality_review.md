# Round 02 quality review

## Scientific accuracy

- Input: `convergence_practice.csv`, 30 rows, three algorithms, ten iterations each.
- Input bytes in the delivery bundle match `examples/data/convergence_practice.csv`.
- Objective direction: minimize; lower values are better.
- `historical_best` is the cumulative minimum within each algorithm.
- `delta_objective = objective[t] - objective[t-1]`; negative values are decreases and positive values are temporary increases under minimization.
- No rows are missing or duplicated, and no observations were smoothed, filtered, normalized, interpolated, or replaced.
- No replicate runs, uncertainty estimates, statistical tests, or confidence intervals are present or drawn.
- The Figure only states what is visible in the displayed practice trajectories. It does not claim speed, stability, robustness, general superiority, or real-experiment performance.

Verified numeric summaries:

| Algorithm | start | end | net change | positive steps |
|---|---:|---:|---:|---|
| Proposed | 1.84 | 0.90 | -0.94 | iteration 6: +0.02 |
| Adaptive | 2.10 | 1.01 | -1.09 | none |
| Baseline | 2.40 | 1.60 | -0.80 | none |

## Nature-first design review

- Fixed Nature Skill version: commit `1930963cbc004da9ac8e3af7944d4f0a3488d3e1`.
- Loaded references: figure contract, design theory, common patterns, Nature observations, and QA contract.
- No new external or personal-gallery reference image was used.
- Chosen structure: full-width hero objective trajectories plus a subordinate signed-update strip.
- Removed the first-round endpoint dumbbell because it repeated information already encoded by the trajectories.
- Replaced the first-round heatmap with a signed line strip to remove the colorbar and reduce cell-by-cell decoding cost.
- Historical-best is shown only at the single Proposed point where it differs from current; coincident values are not overplotted redundantly.

## Engineering verification

- Final `plot.py` was run through `tools/run_figure_task.py` in Conda environment `scientific-figure-studio`.
- Final PNG, SVG, and PDF were generated successfully.
- Ruff passed for `plot.py`, `config.py`, and `qa_previews.py`; all three files also passed `py_compile`.
- Project test suite result: `133 passed in 41.54s`.
- `tools/verify_nature_figure.py` confirmed the pinned Nature Skill commit and 30-file tree.
- Delivered `plot.py`, `config.py`, `figure_brief.json`, and the source data manifest were checked against the authoring inputs; the runner intentionally rewrites the delivered manifest data path to the packaged CSV filename.
- `qa_previews.py` generated 89 mm and 180 mm previews from the final PNG while preserving aspect ratio.
- `comparison.png` was generated by `tools/figure_revision.py compare` from the actual Round 01 and Round 02 PNGs.

## Visual inspection

- Full-size PNG opened: no clipping, panel overlap, or legend obstruction observed.
- 180 mm preview opened: the main trajectories, endpoint labels, local incumbent callout, delta sign convention, and method legend remain readable.
- 89 mm preview opened: no clipping or overlap; the whole evidence chain remains visible, but the title and small annotations are less comfortable. The figure is therefore documented as double-column-targeted.
- One font issue was found and fixed: Unicode subscript glyphs rendered as boxes in the first render. The formula was changed to Matplotlib mathtext subscripts and all exports were regenerated.
- Remaining non-blocking warning: the installed Microsoft YaHei font has no distinct `semibold` face, so Matplotlib falls back to a bold face. This does not change the data or cause clipping.

## Open limitations

The input remains a small illustrative practice table with no experimental uncertainty and no threshold definition. The figure cannot support claims beyond the displayed ten-iteration trajectories. Single-column placement is not the intended final use for this amount of information.
