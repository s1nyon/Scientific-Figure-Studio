# 模型架构示意图模板

这是 `illustrative example structure` 与 `illustrative practice data` 示例，不代表用户真实模型。模板只使用 JSON 中提供的 `modules` 和 `connections`，不会自行添加 encoder、solver、decoder、数据流或性能结论。

运行：

```powershell
python templates/illustration/architecture/plot.py
```

输出目录为 `examples/outputs/fig_09_illustration_architecture/`。修改 `config.py` 的模块尺寸、颜色、箭头、字号或坐标范围后重新运行，保持 PDF、SVG、PNG 和 provenance manifest 同步更新。
