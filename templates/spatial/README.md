# 空间路径与网络优化模板

这是明确标记的 `illustrative practice data` 示例。坐标是二维模拟平面单位，不是真实地理数据，不应解释为地图或地理距离。

运行：

```powershell
python templates/spatial/plot.py
```

图中包含节点、网络边、障碍物、起点、终点和两条候选路线。坐标轴使用等比例显示，箭头表示路线方向。颜色与节点形状共同表达起点、终点和中间节点。

正式使用真实地理数据时，请先确认坐标系、投影、距离单位和障碍物定义，不要直接把经纬度当作平面距离。
